---
# System configuration
default_user: sasho
install_path: "/home/{{ ansible_user | default(default_user) }}/onlyoffice"

# Database configuration
db_password: 09876#Pmf

# ONLYOFFICE configuration
onlyoffice_jwt_secret: Pmf12345#09876Pmf  # Use this same secret in all Nextcloud instances
internal_domain: 192.168.100.46  # Internal IP address for LAN access
trusted_domains: "192.168.100.*"  # Allow all IPs in your internal network range

# Performance and security settings
max_file_size: 1000000000  # 1GB in bytes
max_memory: 512  # Memory limit in MB
max_converter_processes: 10  # Number of simultaneous conversions
verify_peer_off: true  # For self-signed certificates