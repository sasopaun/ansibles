---
# System configuration
default_user: sasho
install_path: "/home/{{ ansible_user | default(default_user) }}/cloud"

# Database configuration
db_name: nextcloud
db_user: nextcloud
db_password: 09876#Pmf

# Nextcloud admin credentials
nextcloud_admin_user: admin
nextcloud_admin_password: Skopje#091

# Network configuration
domain_name: cloud.fixsys.org
trusted_proxy: 192.168.100.35
server_ip: 192.168.100.40

# ONLYOFFICE integration
jwt_secret: Pmf12345#09876Pmf  # Must match onlyoffice_jwt_secret from onlyvault.yml
onlyoffice_server: "192.168.100.46"
verify_ssl: false  # For self-signed certificates

# Additional trusted domains for internal network
additional_trusted_domains:
  - "192.168.100.46"  # ONLYOFFICE server
  - "192.168.100.*"   # Local network range

# Redis configuration
redis_host: redis
redis_port: 6379

# Document handling configuration
php_memory_limit: 512M
upload_max_filesize: 1G
max_execution_time: 300
